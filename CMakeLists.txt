cmake_minimum_required (VERSION 2.8.11)
project (iOSTools)
add_executable (8900strip 8900strip.c)
add_executable (checkdmgencrypt checkdmgencrypt.c)
if (WIN32)
add_executable (genpass genpass.cpp win/getopt.c)
include_directories(win/include)
TARGET_LINK_LIBRARIES(genpass ../win/ssleay32)
TARGET_LINK_LIBRARIES(genpass ../win/libeay32)
else()
add_executable (genpass genpass.cpp)
endif()